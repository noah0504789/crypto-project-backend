spring:
  application:
    name: market-service

  config:
    import: ""

  cloud:
    config:
      enabled: false
      fail-fast: false
      import-check:
        enabled: false
    discovery:
      enabled: false

eureka:
  client:
    enabled: false

api-path:
  market:
    markets: /markets
    price-alerts-me: /price-alerts/me
