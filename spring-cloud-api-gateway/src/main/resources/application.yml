spring:
  application.name: api-gateway
  config.import: configserver:http://crypto-spring-cloud-config:8888
  cloud:
    config:
      name: api-gateway,eureka-client,jwt,frontend,kafka,monitoring
      label: main
