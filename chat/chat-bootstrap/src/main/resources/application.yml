spring:
  application.name: chat-service
  config.import: configserver:http://crypto-spring-cloud-config:8888
  cloud:
    config:
      name: chat-service,eureka-client,mysql,mongo,redis,kafka,monitoring
      label: main