# Task 출력 캐시. clean 후에도 입력이 같은 task 는 재실행하지 않는다.
org.gradle.caching=true

# org.gradle.parallel 은 켜지 않는다.
# BootSmokeTest 가 모듈마다 Testcontainers 를 띄우는데, 병렬 실행 시 컨테이너가 동시에
# 기동되며 리소스 경합으로 기동 타임아웃(LogMessageWaitStrategy)이 난다. 실측에서
# outbox-poller BootSmokeTest 가 깨졌고 전체 시간도 7분 → 13분으로 늘었다.
