spring:
  application.name: outbox-poller
  config.import: configserver:http://crypto-spring-cloud-config:8888
  cloud:
    config:
      name: outbox-poller,eureka-client,mysql,kafka,monitoring
      label: main
