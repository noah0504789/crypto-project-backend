spring:
  application.name: user-service
  config.import: configserver:http://crypto-spring-cloud-config:8888
  cloud:
    config:
      name: user-service,eureka-client,idgen,mysql,monitoring
      label: main
