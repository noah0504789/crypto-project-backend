plugins {
    id 'crypto-common-library'
}

dependencies {
    api project(':user:user-contract')
    api project(':protobuf')

    api platform(libs.grpc.bom)
    api(libs.grpc.stub)
    api(libs.grpc.client.spring.boot.starter)
    implementation(libs.protobuf.java)
}
