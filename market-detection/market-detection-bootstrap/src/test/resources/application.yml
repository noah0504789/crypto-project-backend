spring:
  cloud:
    config:
      enabled: false

price-alert-detection:
  window-minutes: 3
  max-event-age: 10s

  store:
    name: upbit-ticker-store
    retention: 5m
    window-size: 3m
    retain-duplicates: false
