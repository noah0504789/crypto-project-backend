spring:
  application.name: oauth2-client
  config.import: configserver:http://crypto-spring-cloud-config:8888
  cloud:
    config:
      name: oauth2-client,eureka-client,jwt,frontend,kafka,monitoring
      label: main
