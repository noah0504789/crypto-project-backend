spring:
  application.name: oauth2-authorization-server
  config.import: configserver:http://crypto-spring-cloud-config:8888
  cloud:
    config:
      name: oauth2-authorization-server,eureka-client,jwt,redis,monitoring
      label: main
